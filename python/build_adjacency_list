from collections import defaultdict
def build_adjacency_list(edges, ditected=False):
    graph= defaultdict(list)
    for u,v in edges:
        graph[u].append(v)
        if not ditected:
            graph[v].append(u)
    return graph

print(build_adjacency_list([[2,3],[3,1],[3,5],[5,6]]))
